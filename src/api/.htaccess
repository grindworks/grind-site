# src/api/.htaccess
Options -Indexes

# APIを使用しない場合は、以下のコメントを外して直接アクセスを完全に遮断できます
# <IfModule mod_authz_core.c>
#     Require all denied
# </IfModule>
# <IfModule !mod_authz_core.c>
#     Order deny,allow
#     Deny from all
# </IfModule>
